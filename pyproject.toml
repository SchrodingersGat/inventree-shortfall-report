#This file is used to package the ComponentShortfall plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.14.0
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-component-shortfall"
description = "Generate component shortfall reports"
dynamic = ["version"]
authors = [
    { name = "Oliver Walters",email = "oliver.henry.walters@gmail.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/SchrodingersGat/inventree-shortfall-report"

[project.entry-points."inventree_plugins"]
ComponentShortfall = "component_shortfall.core:ComponentShortfall"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "component_shortfall.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["component_shortfall"]
